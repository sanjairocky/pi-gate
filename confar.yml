name: build pi-gate
id: 123

flows:
  default:
    - name: docker build
      shell:
        - docker build ${PWD} -t sanjai/pi-gate:1.0.0 --build-arg CACHEBUST=$(date +%s)
        # - docker
        # - build
        # - .
        # - -t
        # - sanjai/pi-gate:1.0.0
        # - --build-arg
        # - CACHEBUST=$(date +%s)
